<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             xmlns:local="clr-namespace:Intranet.App.ViewModels"    
             x:Class="Intranet.App.Views.LoginViewPage">

    <ContentPage.BindingContext >
        <local:LoginViewModel/>
        
    </ContentPage.BindingContext>
    <ContentPage.Content>
        <StackLayout Padding="50" VerticalOptions="CenterAndExpand">
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Label Text="Usuario" />
                <Entry Text="{Binding UserName}" x:Name="TxtUserName" WidthRequest="150"></Entry>
            </StackLayout>
            <StackLayout Orientation="Horizontal" HorizontalOptions="CenterAndExpand">
                <Label Text="Password" />
                <Entry Text="{Binding Password}" IsPassword="True" x:Name="TxtPasword" WidthRequest="150"></Entry>    
            </StackLayout>
            <Button Command="{Binding LoginCommand}" Text="Iniciar Sesión" HorizontalOptions="CenterAndExpand" VerticalOptions="End" WidthRequest="150"></Button>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>